import axios from "../../axios";
import React, { useState } from "react";
import { useEffect } from "react";

export const Schedule = () => {
  const [sessions, setSessions] = useState([]);

  useEffect(() => {
    // call api to populate sessions
  })

  return <div>Schedule</div>;
};
